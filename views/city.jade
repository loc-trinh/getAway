extends layout

block content
  each destination in results
    if destination.locationData
      a(href=destination.locationData.city)
        img(src=destination.locationData.images[0], width="350px", height="350px")